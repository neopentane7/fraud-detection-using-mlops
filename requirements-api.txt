# Serving-only dependencies. Deliberately minimal so the API container
# stays small and has a tight dependency surface.
fastapi==0.110.*
uvicorn[standard]==0.29.*
pydantic==2.6.*
mlflow==2.13.*
xgboost==2.0.*
scikit-learn==1.4.*       # needed to unpickle the fitted RobustScaler at serving
pandas==2.2.*
numpy==1.26.*
prometheus-fastapi-instrumentator==6.1.*
